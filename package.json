{
  "name": "effects-as-data",
  "version": "2.0.3",
  "description": "Express async workflows using pure functions.",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/orourkedd/effects-as-data"
  },
  "scripts": {
    "test": "jest",
    "test-mocha": "mocha src/**/*.spec.js --recursive --compilers js:babel-register --require babel-polyfill",
    "transpile": "babel src --out-dir lib --source-maps",
    "deploy": "npm run transpile && npm test && npm publish --tag beta"
  },
  "author": "orourkedd",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-jest": "^20.0.0",
    "babel-polyfill": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "jest-cli": "^20.0.0",
    "ramda": "^0.24.1",
    "sinon": "^2.2.0"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "<rootDir>/test.js",
      "<rootDir>/src/test.js",
      "<rootDir>/lib/test.js"
    ]
  }
}
